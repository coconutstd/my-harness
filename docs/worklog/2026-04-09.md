---
id: "worklog-2026-04-09"
type: "worklog"
title: "작업 이력 2026-04-09"
date: "2026-04-09"
priority: "high"
tags: ["harness", "setup"]
updated: "2026-04-09"
---

## 0cd42b0 — Add Worklog, Branching, Task Management harness

### 맥락
3개 신규 하네스를 추가했다.
- Worklog: 커밋 단위로 작업 맥락 기록. 커밋이 없으면 기록도 없다는 원칙 채택.
- Branching: GitHub Flow 규칙을 에이전트에게 전달. strategy.md(정적)와 active-branches.md(동적) 분리.
- Tasks: 사용자가 자유 형식으로 요구사항 제시 → 에이전트가 구조화하여 TASK-NNN.md로 적재.

### 시도했다 포기한 것
- Worklog를 세션 단위로 기록하는 안: 마음에 안 드는 작업을 폐기할 때 기록이 남는 문제가 있어 커밋 단위로 변경.

### 다음 커밋에서 다룰 것
- TASK-001: 실제 Next.js 기능 개발로 3개 하네스 실전 검증

---

## 2c8d8a9 — feat: add about page for harness validation

### 맥락
TASK-001 실전 검증 작업을 수행했다.
- `feature/task-001-harness-validation` 브랜치 생성 (branching 규칙 준수)
- `src/app/about/page.tsx` 페이지 구현 (harness-basic 프로젝트 소개 콘텐츠)
- 커밋 전 TASK-001 상태를 ready → in-progress로 변경
- 브랜치 생성 후 active-branches.md에 행 추가

3개 하네스 동시 실행 검증:
1. **Branching**: strategy.md의 `feature/` 명명 규칙 준수, active-branches.md 실시간 업데이트 완료
2. **Tasks**: TASK-001 상태 변경 및 _index.md 동기화 완료
3. **Worklog**: 이 섹션에 기록됨 (커밋 직후)

### 시도했다 포기한 것
(없음)

### 다음 커밋에서 다룰 것
- TASK-001 완료 처리 (status: done, 완료 기준 체크, _index.md 갱신)
- active-branches.md에서 브랜치를 "최근 병합"으로 이동

---

## 7b8f2d2 — docs: update progress and add ADR-002 after TASK-001 completion

### 맥락
TASK-001 검증 결과를 공식 문서화했다.

- `docs/progress/current.md` 갱신: Harness 실전 검증 완료 상태 반영
- `docs/decisions/ADR-002-harness-validation-success.md` 신규 생성: 3개 하네스 정상 동작 확인 기록
- `docs/INDEX.md` 갱신: 새 ADR 및 progress 변경 반영

CLAUDE.md의 규칙 준수:
> 작업이 완료되거나 새로운 결정이 내려지면:
> - 관련 progress/ 파일 업데이트 ✅
> - 새 결정은 decisions/ 파일 생성 ✅
> - INDEX.md 갱신 ✅

### 시도했다 포기한 것
(없음)

### 다음 커밋에서 다룰 것
(없음)

---

## 79fa1a0 — docs: add explicit checklist for post-completion harness procedures

### 맥락
사용자가 지적했듯이, 이전 작업에서 3단계(Worklog/Branching/Tasks) 외에
progress/decisions/INDEX 관리가 한 번에 동작하지 않았다.

원인: CLAUDE.md의 "진행 상태 업데이트" 섹션이 규칙을 정의했지만,
**언제/어떤 순서로 실행할지** 명확하지 않았다.

개선 사항:
- Phase 1/2/3으로 단계 구분 (시간순)
- 각 Phase별 구체적인 파일과 작업 명시
- 체크리스트 템플릿 추가 (에이전트가 빠뜨리지 않도록)
- Phase 3 내에서도 조건별(새 결정/진행 상태/파일 갱신) 명시

→ 다음 작업부터는 이 체크리스트를 따르면
한 번에 모든 하네스가 정상 동작할 것으로 예상된다.

### 시도했다 포기한 것
(없음)

### 다음 커밋에서 다룰 것
(없음)

---

## 90581c0 — Add Context Harness structure

### 맥락
harness-basic 프로젝트에 Context Harness 패턴을 도입했다.
에이전트가 세션 시작 시 `docs/INDEX.md`를 읽고 필요한 컨텍스트만 선택적으로 읽는 구조.
CLAUDE.md에 필수 절차를 명시하여 에이전트가 규칙을 따르도록 강제.

### 시도했다 포기한 것
- CLAUDE.md 하나에 모든 컨텍스트를 넣는 방식: 컨텍스트가 늘어날수록 매 세션마다 전부 로드하게 되어 비효율적. 포기.

### 다음 커밋에서 다룰 것
- Worklog / Branching / Task Management 하네스 추가
- 실제 Next.js 기능을 개발하며 패턴 실전 검증

---

## 8b8d07b — docs: record worklog for 7b8f2d2

### 맥락
7b8f2d2 커밋의 worklog를 기록했다.

### 시도했다 포기한 것
(없음)

### 다음 커밋에서 다룰 것
(없음)

---

## 172e75a — docs: record worklog for 79fa1a0

### 맥락
79fa1a0 커밋의 worklog를 기록했다.

### 시도했다 포기한 것
(없음)

### 다음 커밋에서 다룰 것
(없음)

---

## 3d79a31 — docs: finalize TASK-001 completion status

### 맥락
TASK-001 완료를 최종 확정했다.
docs/tasks/TASK-001.md와 _index.md의 상태 변경(done)을 명확히 커밋하고,
PR 전에 모든 변경사항이 스테이징/커밋되도록 정리했다.

### 시도했다 포기한 것
(없음)

### 다음 커밋에서 다룰 것
(없음)

---

## 011be3e — docs: finalize TASK-002 harness sync

### 맥락
기존 `docs/task-002-agents-md-and-rename` 브랜치에 대응하는 태스크 문서가 누락되어 있어
하네스 문서 간 상태가 어긋나 있었다.

- `docs/tasks/TASK-002.md` 신규 작성
- `docs/tasks/_index.md`에 TASK-002 추가 및 `done` 반영
- `docs/progress/current.md`, `docs/INDEX.md`를 TASK-002 완료 기준으로 갱신
- `docs/branching/active-branches.md`에서 TASK-002 브랜치를 최근 병합 목록으로 정리

### 시도했다 포기한 것
- 별도 `TASK-003` 생성: 기존 브랜치 명명과 이력이 이미 `TASK-002`를 사용하고 있어 번호를 늘리는 대신 TASK-002를 마무리하는 쪽이 일관적이라 포기.

### 다음 커밋에서 다룰 것
- 이 커밋의 worklog 기록

---

## 46b2c0c — feat: add harness consistency check

### 맥락
하네스 운영에서 반복적으로 발생한 불일치를 문서 규칙만으로는 막기 어려워
로컬 검증 스크립트를 추가했다.

- `scripts/harness-check.mjs` 추가
- `package.json`에 `npm run harness:check` 연결
- `CLAUDE.md`에 태스크 번호 중복 확인 및 완료 전 검증 실행 규칙 추가
- `docs/architecture/tech-stack.md`에 스크립트 위치와 검증 명령 반영
- `TASK-003` 및 관련 `progress/branching/index` 문서를 구현 완료 상태로 동기화

현재 스크립트는 다음을 검사한다:
- 현재 브랜치와 `docs/branching/active-branches.md` 정합성
- `docs/tasks/_index.md`와 `docs/tasks/TASK-*.md`의 제목/상태/우선순위 정합성
- `docs/progress/current.md`와 `docs/INDEX.md`의 최신 상태 설명 정합성

### 시도했다 포기한 것
- 스킬만으로 해결: 절차 안내는 가능하지만 Git/문서 상태 불일치를 기계적으로 막지는 못해 스크립트를 우선 선택.

### 다음 커밋에서 다룰 것
- 이 커밋의 worklog 기록

---

## 4dff4fd — feat: derive branch status from git

### 맥락
브랜치 상태를 `docs/branching/active-branches.md` 같은 수동 문서에 기록하는 방식은
GitHub에서 PR을 직접 merge할 때마다 쉽게 어긋났다.

이번 커밋에서 운영 모델을 바꿨다.

- `docs/branching/active-branches.md` 제거
- `scripts/harness-sync.mjs` 추가, `npm run harness:sync`로 현재/병합 브랜치 상태 출력
- `scripts/harness-check.mjs`를 브랜치 상태 문서 의존 없이 동작하도록 수정
- `CLAUDE.md`, `docs/branching/strategy.md`, `docs/INDEX.md`를 Git 직접 조회 기반 규칙으로 갱신
- `TASK-004`와 관련 progress/index 문서를 완료 상태로 동기화

핵심 방향:
- 실시간 상태는 Git에서 읽는다
- docs에는 스펙, 진행 상태, 태스크, worklog 같은 이력성 정보만 남긴다

### 시도했다 포기한 것
- merge 후마다 `active-branches.md`를 별도 커밋으로 정리하는 방식: `main`에 상태성 커밋이 계속 쌓이고, GitHub에서 직접 merge하면 다시 누락되기 쉬워 포기.

### 다음 커밋에서 다룰 것
- 이 커밋의 worklog 기록

---

## 22fee6d — docs: add nextjs frontend agent harness

### 맥락
기존 `.agents/nextjs-frontend-architect.md`는 이 저장소와 맞지 않는 전제를 많이 포함하고 있었다.

- `pnpm`, `TanStack Query`, `Axios`, 특정 백엔드 구조 같은 외부 전제를 제거했다
- 이 저장소 실제 상태인 Next.js 15, React 19, npm, App Router, Context Harness 절차를 기준으로 새 `.agents/frontend-dev.md`를 작성했다
- Claude Code가 읽을 수 있도록 `.claude/agents/frontend-dev.md` 심볼릭 링크를 추가했다
- `docs/architecture/nextjs-agent-harness.md`와 `TASK-005`를 통해 사용 기준과 변경 이력을 문서화했다

핵심 방향:
- 에이전트 문서는 범용 템플릿보다 저장소 현실과 먼저 맞아야 한다
- 프론트엔드 구현 규칙도 `docs/INDEX.md`와 `CLAUDE.md` 흐름을 따라야 한다

### 시도했다 포기한 것
- 기존 `nextjs-frontend-architect` 문안을 부분 수정하는 방식: 저장소와 무관한 전제가 너무 많아 정리 비용이 커서, 레포 전용 `frontend-dev`로 교체하는 쪽이 더 명확해 포기.

### 다음 커밋에서 다룰 것
- 이 커밋의 worklog 기록

---

## 830189f — feat: 홈 페이지를 투두앱으로 교체

### 맥락
TASK-006 요구사항에 따라 기본 Next.js 스캐폴딩 페이지를 실제 투두앱으로 교체했다.

- `src/components/TodoApp.tsx` 신규 작성 (Client Component)
  - useState로 로컬 상태 관리, useEffect로 localStorage 동기화
  - 할 일 추가/완료토글/삭제 기능 구현
  - Enter 키 지원, 빈 문자열 추가 방지
  - Tailwind CSS 4의 불투명도 유틸리티로 다크/라이트 테마 대응
  - `mounted` 상태로 hydration mismatch 방지 (localStorage는 클라이언트에서만 읽힘)
- `src/app/page.tsx`를 TodoApp 단순 임포트로 교체
- `src/app/layout.tsx` 메타데이터를 투두앱 기준으로 변경
- `src/app/about/page.tsx` 삭제 (TASK-001 검증용 테스트 페이지였음)

Server Component인 page.tsx에서 Client Component인 TodoApp을 임포트하여 서버/클라이언트 경계를 명확히 분리했다.

### 시도했다 포기한 것
- TodoItem 타입을 별도 `src/types/todo.ts`로 분리: 현재는 컴포넌트 단일 파일로 충분해 인라인 정의 유지.

### 다음 커밋에서 다룰 것
- TASK-006 완료 처리 및 하네스 루틴 문서 갱신
